domain = 'doichi no tenkai'
print(domain.upper())

gojo = 'gojo satoru'
print(gojo.replace('gojo satoru', 'yuta okutsu'))

print(domain.capitalize())

print('String are immutable')
# it means variables are not changeable
# for example: domain

# the use  of find is to find the index of the string
print(gojo.find('s'))
# also find must have always has a value inside the parenthesis else it will throw some error

# the use of split is to split the string
print(gojo.split(' '))
